import React from 'react';
import { Box, Typography } from '@mui/material';

const Workouts: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>אימונים</Typography>
      <Typography variant="body1">ניהול תוכניות אימון</Typography>
    </Box>
  );
};

export default Workouts;
