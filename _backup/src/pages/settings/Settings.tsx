import React from 'react';
import { Box, Typography } from '@mui/material';

const Settings: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>הגדרות</Typography>
      <Typography variant="body1">הגדרות מערכת</Typography>
    </Box>
  );
};

export default Settings;
