import React from 'react';
import { Box, Typography } from '@mui/material';

const Clients: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>מתאמנים</Typography>
      <Typography variant="body1">רשימת המתאמנים שלך</Typography>
    </Box>
  );
};

export default Clients;
