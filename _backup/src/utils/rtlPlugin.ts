import rtlPlugin from 'stylis-plugin-rtl';

/**
 * RTL plugin for Emotion CSS-in-JS library to support right-to-left text direction
 * Used by theme provider to properly render Hebrew text and layout
 */
export default rtlPlugin;
